default: &default
  adapter:  postgresql
  host:     localhost
  username: markstephenson
  password:

development:
  <<: *default
  database: botevents-dev

test:
  <<: *default
  database: botevents-test
